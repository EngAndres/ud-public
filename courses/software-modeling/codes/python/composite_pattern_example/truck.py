from composite import Item, Product, Box

class Truck:

    def __init__(self, MAX_WEIGHT: float, MAX_VOLUME: float):
        self.MAX_WEIGHT = MAX_WEIGHT
        self.MAX_VOLUME = MAX_VOLUME
        self.items = []

    

if "__main__" == __name__:
    while True:
        